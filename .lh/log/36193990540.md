# Run 36193990540

- date: 2026-09-25T22:11Z
- target: web
- issue: 56
- model: grok-4.7-high
- trigger: issues on main
- branch resumed: false
- checks: success
- state.md: ok

## Files changed

     .lh/web/state.md                | 31 ++++++++++++++++---------------
     web/components/ad-demo-feed.tsx | 23 +++++++++++++----------
     2 files changed, 29 insertions(+), 25 deletions(-)

## Agent summary (tail)

    What changed
    - The ad frame in `web/components/ad-demo-feed.tsx` was collapsing to a few pixels because its height used a percentage of a parent that sized itself to the frame. It now uses `calc(100svh - 5.5rem)`,
    - The ad-state column is a real scrollport. The feed row is capped to the viewport, and those cards no longer shrink and clip. On a short desktop pane the column scrolls through to Liquid fold.
    - Below the `lg` breakpoint, snap is off, so the state cards under each creative scroll with the feed. Previous/next sit mid-frame and no longer cover the call-to-action.
    - `npm run lint` and `npm run build` passed. A headless browser checked desktop and phone scrolling.
    
    Next run
    - Show the campaign next-day brief in the ad-state column without implying lift.
    - The signals question (which inputs are measured versus derived) is still open.Chrome and Chromium are available on this machine. That is what was used to check the feed: the creative frame renders a
